# Python
__pycache__/
*.pyc
*.egg-info/

# Training outputs (checkpoints, logs, videos) — large binaries, regenerable
runs/
runs_ppo/
*.pt
*.pth
*.mp4
*.gif
# ...except committed showcase assets referenced by README/docs (not regenerable training output)
!docs/assets/*.gif

# Web GUI job-tracking state (pidfiles, job metadata) — see webui/jobs.py
webui_state/

# OS
.DS_Store

# Local Claude Code permissions (machine-specific)
.claude/settings.local.json
